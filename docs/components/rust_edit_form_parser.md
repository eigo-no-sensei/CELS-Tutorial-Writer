# Rust edit-form parser — active component spec

> Generated from `contracts/component_boundaries.json`. Do not edit this file directly.

**Component ID:** `rust_edit_form_parser`  
**Status:** `active`

## Role

- Pure parser from historical GEL edit HTML to raw/effective edit-form state.

## Scope

- Parse historical GEL edit HTML into a raw structural representation without inventing missing controls.
- Recover browser-effective select values, including inline-JS selection and browser default-first-option semantics.
- Recover Aims contenteditable separately from its hidden backing input and recover historical teacher selection evidence.

## Authority

- Derived parser output; not authoritative for historical absent because of ANOM-ABSENT-001.

## Reads

- `gel_edit_html`
- `repository_contracts`

## Writes

- `parsed_edit_form_state`

## Must not write

- `live_gel_tutorials`
- `normalized_local_archive`
- `repository_contracts`
- `tutorial_post_payload`

## State transitions

- Each parse creates fresh derived edit state; source HTML change invalidates prior output.

## Failure semantics

- HTML/syntax parsing errors fail parsing.
- Missing or structurally unexpected tutorial controls remain observable in raw parsed state; the validator, not the parser, decides contract validity.

## Dependencies

- contract_tooling generated gel_fields
- scraper
- regex

## Tests

- `gel-core/tests/edit_form.rs`
