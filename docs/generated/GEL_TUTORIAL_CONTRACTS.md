# GEL tutorial contracts (generated)

> Generated from `contracts/*.json`. Do not edit this file directly.

## Tutorial types

| Semantic type | GEL `ttype` | Display |
| --- | ---: | --- |
| `standard` | `0` | Standard |
| `final` | `1` | Final |
| `initial` | `2` | Initial |

Canonical identity is `tutorial_id` from the tutorial-list API. `(student_uid, tutorial_ts, tutorial_type)` is only a locator/reconciliation key.

## Form fields

| Semantic field | GEL field | Control | Applies to | Required edit control | Unset allowed | Validation | Summary label |
| --- | --- | --- | --- | ---: | ---: | --- | --- |
| `teacher_id` | `tid` | `select` | initial, standard, final | yes | no | `positive_i64_option` | Teacher |
| `student_uid` | `uid` | `hidden` | initial, standard, final | yes | no | `positive_i64` | — |
| `source_timestamp` | `datetime` | `hidden` | initial, standard, final | yes | no | `positive_i64` | — |
| `tutorial_type` | `ttype` | `hidden` | initial, standard, final | yes | no | `tutorial_type_code` | Type |
| `language` | `lang` | `hidden_or_mapper_constant` | initial, standard, final | — | — | `—` | — |
| `absent` | `absent` | `checkbox` | initial, standard, final | yes | no | `checkbox_boolean` | absent |
| `tutorial_date` | `customdate` | `text` | initial, standard, final | yes | no | `gel_date` | — |
| `overall_level` | `dropdown-475` | `select` | initial, standard, final | yes | yes | `none; domain=overall_level` | Tutorial Overall Level |
| `initial_speaking` | `dropdown-264` | `select` | initial, final | yes | yes | `none; domain=skill_level` | Initial Speaking |
| `initial_use_of_english` | `dropdown-265` | `select` | initial, final | yes | yes | `none; domain=skill_level` | Initial Use of English |
| `initial_writing` | `dropdown-266` | `select` | initial, final | yes | yes | `none; domain=skill_level` | Initial Writing |
| `initial_listening` | `dropdown-267` | `select` | initial, final | yes | yes | `none; domain=skill_level` | Initial Listening |
| `speaking` | `dropdown-233` | `select` | standard, final | yes | yes | `none; domain=skill_level` | Speaking |
| `use_of_english` | `dropdown-234` | `select` | standard, final | yes | yes | `none; domain=skill_level` | Use of English |
| `writing` | `dropdown-235` | `select` | standard, final | yes | yes | `none; domain=skill_level` | Writing |
| `listening` | `dropdown-236` | `select` | standard, final | yes | yes | `none; domain=skill_level` | Listening |
| `reading` | `dropdown-476` | `select` | standard, final | yes | yes | `none; domain=skill_level` | Reading |
| `assessment_listening` | `sl-89` | `radio_group` | standard | yes | yes | `none` | Assessment |
| `assessment_reading` | `sr-89` | `radio_group` | standard | yes | yes | `none` | Assessment |
| `assessment_writing` | `sw-89` | `radio_group` | standard | yes | yes | `none` | Assessment |
| `assessment_speaking` | `ss-89` | `radio_group` | standard | yes | yes | `none` | Assessment |
| `assessment_vocabulary` | `sv-89` | `radio_group` | standard | yes | yes | `none` | Assessment |
| `assessment_grammar` | `sg-89` | `radio_group` | standard | yes | yes | `none` | Assessment |
| `assessment_pronunciation` | `sp-89` | `radio_group` | standard | yes | yes | `none` | Assessment |
| `aims` | `trecs-79` | `contenteditable_plus_hidden` | standard | yes | yes | `none` | Aims |
| `teacher_comments` | `text-225` | `textarea` | initial, standard, final | yes | yes | `none` | Teacher's Comments |
| `additional_comments` | `text-226` | `textarea` | final | yes | yes | `none` | Additional Comments/Accommodation (under 18s only) |
| `exam_intent` | `dropdown-413` | `select` | initial | yes | yes | `none` | Do you want to take an English proficiency exam? |
| `exam_type` | `dropdown-414` | `select` | initial | yes | yes | `none` | If yes, which exam? |
| `exam_when` | `dropdown-415` | `select` | initial | yes | yes | `none` | If yes, when do you want to take the exam? |

## Semantic applicability

**Common:** `teacher_id`, `student_uid`, `source_timestamp`, `tutorial_type`, `absent`, `tutorial_date`, `overall_level`, `teacher_comments`

**Initial:** `initial_speaking`, `initial_use_of_english`, `initial_writing`, `initial_listening`, `exam_intent`, `exam_type`, `exam_when`

**Standard:** `speaking`, `use_of_english`, `writing`, `listening`, `reading`, `assessment_listening`, `assessment_reading`, `assessment_writing`, `assessment_speaking`, `assessment_vocabulary`, `assessment_grammar`, `assessment_pronunciation`, `aims`

**Final:** `initial_speaking`, `initial_use_of_english`, `initial_writing`, `initial_listening`, `speaking`, `use_of_english`, `writing`, `listening`, `reading`, `additional_comments`

## Active source anomalies

### ANOM-ABSENT-001 — `absent`

Historical edit form can return checked=true while summary/archive says present. In the 14-fixture audit six such false-positive edit states were observed and no false-negative edit state was observed.

**Authority rule:** For historical revision, summary/archive absent is authoritative; retain edit checkbox value only as provenance/discrepancy evidence.

**Failure rule:** A new mismatch shape is not automatically accepted; strict fixture gate must fail until reviewed.

### ANOM-COLLISION-001 — `tutorial_identity`

Multiple canonical tutorial IDs can share the same student_uid, tutorial_ts and tutorial_type.

**Authority rule:** tutorial_id from tutorial-list API remains canonical identity; collision candidate source states remain group-owned and are never positionally assigned by list order.

**Failure rule:** Divergent, incomplete, or legacy ambiguous-unknown collision state must not be treated as proven per-ID ownership and must block revision-by-ID.

### ANOM-COLLISION-002 — `summary_state`

One observed same-timestamp collision has two distinct summary states (present vs absent) while edit/print timestamp route resolves to only the absent state.

**Authority rule:** Preserve all API IDs and all source states separately. Timestamp-only route state is group evidence, not a discriminator. Identical candidate states may be used only through shared-identical authority; divergent association remains ambiguous.

**Failure rule:** Revision-by-ID of a divergent ambiguous collision fails closed until a future proven discriminator changes the governed association authority.

### ANOM-AIMS-001 — `aims`

Historical Standard edit form can have blank hidden input trecs-79 while visible #tcinput contenteditable contains the historical aims.

**Authority rule:** Historical semantic Aims text comes from #tcinput contenteditable; retain source editor HTML and hidden-input state only as provenance/raw evidence, never as alternate editable values.

### ANOM-BLANK-001 — `summary_blank`

Print fallback can render placeholders such as none for a field that is explicitly blank in summary.

**Authority rule:** Summary field presence is authoritative even when blank; print may supplement only a missing summary key.

### ANOM-LEVEL-001 — `overall_level`

GEL overall-level option uses Ao: beginner while skill-level selects use A0.

**Authority rule:** Normalize Ao to semantic A0; retain the literal overall-level GEL value only as source/provenance evidence, never as an editable or POST fallback value.

### ANOM-ASSESSMENT-001 — `assessment`

Summary renders seven assessment values as one composite row and may contain presentation artefacts; edit form exposes seven structured radio groups.

**Authority rule:** For exact form reconstruction prefer structured edit-form radio values; archive may parse summary composite when edit evidence is unavailable.

## Semantic form authority

Draft origins are explicit: `revision` requires a historical source identity; `new` may retain an optional prepopulation source but is not a revision identity.

Editable source-of-truth rules:

- `overall_level`: `semantic_cefr_level_only`
- `aims`: `plain_text_only`
- `absent_revision`: `summary_archive_authoritative`
- `reading`: `semantic_cefr_level_only`

Provenance-only values are diagnostics/fidelity data and must not drive POST payload values:

- `overall_level_source_raw`: Retain the literal GEL select value for diagnostics; never use it as POST fallback.
- `aims_source_html`: Retain historical #tcinput inner HTML for fidelity; never use it as the editable Aims value or POST fallback.
- `edit_form_absent`: Retain the historical edit checkbox value and discrepancy marker; never override summary/archive absent on revision construction.

Aims POST encoding: `html_escape_then_newlines_to_br`.

Revision construction must fail closed before semantic state creation when the historical state authority does not permit revision-by-ID.

## Collision authority

tutorial_id from the tutorial-list API is canonical identity. student_uid+tutorial_ts+tutorial_type is only a reconciliation locator and must never be promoted to identity.

Captured summary states remain separate from canonical API identities unless the reconciliation result proves a per-tutorial association. Collision handling must never assign one ambiguous summary column to a specific API tutorial_id merely by list order.

### `identical_state`

A collision group with equal non-zero API and summary counts in which every captured summary state has the same governed state content.

Per-ID association: **ambiguous**.

Revision rule: Revision by tutorial_id may use the common shared state because all candidate states are equivalent, while retaining shared-identical collision provenance and preserving every canonical tutorial_id separately.

### `divergent_state`

A collision group with equal non-zero API and summary counts and more than one governed summary state.

Per-ID association: **ambiguous**.

Revision rule: Revision by tutorial_id is blocked until a proven discriminator associates a specific state with that tutorial_id.

### `incomplete_evidence`

A collision group whose API and summary counts differ or whose candidate summary evidence is absent.

Per-ID association: **unproven**.

Revision rule: Revision by tutorial_id is blocked.

Timestamp-route rule: Timestamp-only print/edit route observations are collision-group evidence. They must not be attached to a particular canonical tutorial_id as a discriminator unless a future source proves that association.

Legacy archive rule: Until D1 persists collision kind and state associations explicitly, any legacy archive row marked collision_ambiguous is not sufficient evidence for revision-by-ID. It must be treated as ambiguous_unknown and fail closed unless separately resolved from governed collision evidence.

## Offline POST round trip

For each private revision fixture, the derived POST payload must exactly equal an independently reconstructed expected successful-control map. No extra or missing key is permitted.

Strict C4 evidence requires 14 revision fixtures, including 5 Final fixtures and 6 governed absent source discrepancies.

Historical absent rule: The historical edit checkbox may disagree with archive absent. Round-trip expectation uses archive absent and requires the discrepancy to remain provenance only.

Mutation isolation: Changing one editable semantic value in an otherwise valid state may change only its declared GEL payload field; changing provenance-only fields must change no payload field.

Network rule: This phase grants no HTTP client, session, or GEL write authority. Round-trip/N2a serialization validation is offline only.

## Archive schema v2

The executable SQLite schema and its migration tests are higher authority than descriptive archive documentation. D1 introduced schema v2 without transferring runtime ownership; A2 now makes RustArchiver the production orchestrator and the crate-private ArchiveSyncRepository the sole canonical local SQLite write capability.

Executable schema: `schema/archive_v2.sql`; schema version: **2**.

Key executable invariants:

- `tutorial_identities` owns canonical API tutorial IDs; `tutorial_source_states` owns candidate historical states; `tutorial_state_associations` carries governed authority.
- Blocked authority statuses persist no `source_state_id`, so ambiguous collision candidates cannot be falsely attributed to an API ID.
- `students.class_id` is replaced by `class_memberships`.
- `source_present`, `first_seen`, and `last_seen` preserve history while representing current-source presence; `presence_checked_run_id` may invalidate presence only against a completed sync run.
- Foreign keys are mandatory and `INSERT OR REPLACE` is forbidden in favor of explicit `ON CONFLICT` UPSERTs.
- v2 stores no legacy `raw_json` or email field; migration extracts only minimal field-presence provenance.

Runtime cutover: `a2_rust_archiver_active_pending_strict_acceptance`.

## Rust archive repository (D2)

A2 transfers production canonical local-archive ownership to the Rust archiver. RustArchiver acquires a complete GEL snapshot before transactional materialization, may atomically create a fresh schema-v2 database, and delegates all SQLite mutation to the crate-private ArchiveSyncRepository. Python v4.9 is retained only as historical/reference parity authority.

Public read surface: `gel-core/src/archive_repository.rs` using `existing schema-v2 SQLite database; SQLITE_OPEN_READ_ONLY plus PRAGMA query_only=ON`.

Archive-sync write surface: `gel-core/src/archive_sync_repository.rs`; visibility `crate_private`; runtime status `production_canonical_A2`.

Transaction rule: All candidate archive writes for one repository sync operation occur in one SQLite transaction.

Full invalidation: A successful fully-evidenced full sync may mark unseen classes, students, memberships, tutorial identities, source states and collision groups source_present=0 using presence_checked_run_id for that completed run. Rows are preserved, never deleted.

Targeted invalidation: Targeted syncs never infer absence and never mark unseen source rows not present.

Production writer after A2: `rust_archive_sync_repository` via `gel-core/src/archiver.rs#RustArchiver`; Python v4.9 status is `historical_reference`.

## Rust GEL read-only session (N1)

N1 remains a narrow authenticated acquisition boundary. N2b extends only the fixed GET surface with current New-tutorial form acquisition using the canonical zero-locator route; credentials/cookies remain session-private, student email fields are stripped at acquisition, and no generic request or GEL tutorial mutation API exists. Login remains the only POST authority.

Canonical origins: `https://api2.guidedelearning.net` and `https://learn2.guidedelearning.net`.

Authentication uses exactly 2 login POSTs; credential retention is `none`.

Fixed read surface:

- `GET /staff/classes?open=true` -> `json`
- `GET /staff/classes/{class_id}/students` -> `json_privacy_filtered`
- `GET /staff/students/{student_uid}` -> `json_privacy_filtered`
- `GET /staff/students/{student_uid}/tutorials` -> `json_and_typed_identity_projection`
- `GET /study/tutorials/summary/{student_uid}` -> `html`
- `GET /study/tutorials/print/{student_uid}/{tutorial_ts}` -> `html`
- `GET /study/tutorials/add/{student_uid}/0/{ttype_raw}` -> `html_new_form_source`
- `GET /study/tutorials/add/{student_uid}/{tutorial_ts}/{ttype_raw}` -> `html`
- `POST /administration/students` -> `html_privacy_filtered`

N1/N2b grant fixed read acquisition only. The New-form zero-locator GET is source acquisition, not create authority. No create, revise, delete or arbitrary transport method is exposed.

Student email-like fields are removed recursively before student roster/profile JSON leaves the session component.

Live acceptance is deliberately manual: the automated release gate performs no credentialed network access.

## N2-PW1 zero-write browser evidence

Observe current GEL New/Edit form structure and authentic browser serialization while preventing every GEL tutorial mutation from reaching the server.

The mutation firewall is installed before login; the login request body/headers are never inspected. During evidence collection, the authentic tutorial process POST is reduced to allowlisted structural relationships and always aborted before reaching GEL.

Prepopulation source: `latest_tutorial` (`established_before_PW1`).

Teacher-selection client serialization may be observed, but server acceptance/authorization of reassignment remains explicitly unresolved and PW1 cannot enable production reassignment.

Offline release check: `python -S tools/check_n2_playwright_zero_write.py`. The release gate never executes credentialed Playwright network activity.

## N2a semantic / POST authority

Define New/Revision semantic identity, teacher authority, source-vs-derived ownership and offline POST serialization. No GEL network write.

Origin rules:

- New datetime: `absent_key`; teacher: `immutable opaque ValidatedNewFormTeacher constructed only from a validated N2b hidden tid source control`.
- Revision datetime: `exact source tutorial_ts`; teacher: `immutable_historical_teacher`.

New submission type has no datetime member/path. Revision submission derives datetime only from its source identity.

Network write authority: `True`.

## N2b New-tutorial form authority

Validate the live GEL New-tutorial source form as immutable creation-source authority without weakening historical Revision edit-form validation.

Fixed acquisition: `GET /study/tutorials/add/{student_uid}/0/{ttype_raw}`.

New form source contract: `POST /study/tutorials/process`; forbidden fields: `datetime`.

Teacher authority is a validated hidden `tid` supplied by the fetched New form; no public teacher mutation/constructor is authorized.

Network write authority: `False`.

## Frozen evidence

- **EVID-FIXTURE-270 (observed):** Post-v4.9 14-fixture comparator before Final Reading was added to comparator scope.
- **EVID-FINAL-READING (observed):** All five captured Final edit forms contain enabled select dropdown-476; four have selected CEFR values and one is unset at Please choose level.
- **EVID-COLLISIONS (observed):** v4.9 archive run retained 14 same-timestamp collision groups affecting 29 API rows; 13 groups had one distinct complete summary state and one group had two distinct states.
- **EVID-TEACHERS (observed):** v4.9 archive run recovered historical teacher IDs for all archived tutorials.
- **EVID-ARCHIVE-V49 (observed):** v4.9 archive refresh completed with summary primary and blank-fidelity repair.
- **EVID-FIXTURE-275 (observed):** C1 strict release gate executed after Final Reading was added to comparator scope; exact private-fixture baseline passed.
- **EVID-C2-STRICT (observed):** C2 strict release gate passed after v8.1 with all 14 private fixtures constructing valid Revision semantic states in addition to the frozen 14/275/269/6/0 parity contract.
- **EVID-C3-STRICT (observed):** C3 strict release gate passed after v9.1 with both private collision-authority fixtures proving group-owned candidate states, shared-identical revision authority, and divergent revision blocking.
- **EVID-C4-STRICT (observed):** C4 strict release gate passed after v10.1 with all 14 private revision fixtures producing exact independently reconstructed POST payloads, including all five Final Reading payloads and six archive-authoritative absent discrepancies.
- **EVID-D1-STRICT (observed):** D1 strict release gate passed after v11.1 with archive schema v2 fresh/migration/FK/collision/privacy/UPSERT invariants plus all previously governed private evidence.
- **EVID-D2-STRICT (observed):** D2 strict release gate passed with Rust archive repository boundary and all prior evidence gates.
- **EVID-N1-STRICT-LIVE (observed):** N1 strict release gate and credentialed read-only live parity probe passed; live probe performed zero GEL tutorial writes.
- **EVID-N2-PW1-LIVE (observed_core_authority_with_nonblocking_residual):** Live zero-write PW1 inspected New Standard/Initial/Final and an existing Revision. New forms all omitted datetime, supplied current-date customdate and hidden authenticated-teacher tid; Revision exposed an editable historical-teacher select with 55 options. No GEL tutorial mutation was forwarded and before/after locator/semantic digests were unchanged. Authentic changed-tid serialization was not observed and is deferred as non-blocking.
- **EVID-N2-FIELD-IDENTITY (established_project_authority):** Canonical GEL level-field identity is fixed by field ID/semantic role. Initial uses 264/265/266/267; Standard/current uses 233/234/235/236 plus Reading 476; Final pairs initial/current 264+233, 265+234, 266+235, 267+236. Equal values never establish cross-field provenance.

### Strict private-fixture baseline

Status: **confirmed_observed**.

The strict private-fixture gate was executed on 2026-08-24 and exactly matched the frozen 14/275/269/6/0 contract.

Expected: 14 fixtures, 275 fields, 269 OK, 6 known mismatches, 0 unavailable.
