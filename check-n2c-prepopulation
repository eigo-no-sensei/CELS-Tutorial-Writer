#!/usr/bin/env python3
from pathlib import Path
import subprocess
import sys
root = Path(__file__).resolve().parent
args = [] + sys.argv[1:]
raise SystemExit(subprocess.run([sys.executable, '-S', str(root / 'tools/check_n2c_prepopulation.py'), *args], cwd=root).returncode)
