#!/usr/bin/env python3
from pathlib import Path
import subprocess, sys
root=Path(__file__).resolve().parent
raise SystemExit(subprocess.run([sys.executable, "-S", str(root / "tools" / "check_ui1c2_4_semantic_forms.py")], cwd=root).returncode)
