#!/usr/bin/env python3
from pathlib import Path
import subprocess
import sys
root = Path(__file__).resolve().parent
args = ['--mode', 'source'] + sys.argv[1:]
raise SystemExit(subprocess.run([sys.executable, '-S', str(root / 'tools/check_intake.py'), *args], cwd=root).returncode)
