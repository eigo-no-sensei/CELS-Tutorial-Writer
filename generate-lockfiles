#!/usr/bin/env python3
from pathlib import Path
import subprocess
import sys
root = Path(__file__).resolve().parent
raise SystemExit(subprocess.run([sys.executable, str(root / "tools" / "generate_dependency_lockfiles.py")], cwd=root).returncode)
